name: Front CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: myapp-front-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: self-hosted

    defaults:
      run:
        shell: bash

    env:
      IMAGE_NAME: myapp-front
      IMAGE_TAG: ${{ github.sha }}
      MYAPP_INFRA_DIR: /home/um/myApp-Infra
      DRAIN_SECONDS: 10
      STABILIZATION_SECONDS: 30
      CHECK_INTERVAL_SECONDS: 2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify deployment environment
        run: |
          docker version
          docker network inspect myapp-network >/dev/null
          docker inspect myapp-nginx >/dev/null
          test -d /home/um/myApp-Infra
          test -f /home/um/myApp-Infra/scripts/deploy-front.sh

      - name: Deploy Front
        run: |
          chmod +x scripts/*.sh
          ./scripts/deploy-local.sh