# 메인화면(`https://127.0.0.1:5173/`) 설명 :

## 메인화면은 총 30개의 Level을 가진 게임 리스트를 보여주는 화면이다. 

1. `#appView`은 `width`가 `100vw`이고 `height`가 `100vh`다. 브라우저에서 제공하는 url 같은 영역에 가려져서는 안된다.
* 메인화면 진입 시, 한 장의 악보가 보인다.
- 일반적인 악보와 마찬가지로 여러 줄의 오선보가 있다.
* 오선보에는 `lv1` ~ `lv30` 까지를 나타내는 30개의 음표가 있다.
- 일반적인 음표에 있는 타원 동그라미안에 각 level의 썸네일이 그려져있다.
- `lv1` ~ `lv30`의 코드를 분석해서, 음표의 동그라미안에 각 level에 해당하는 썸네일을 그려 넣어야 한다.
- 단, 아직 Clear 하지 못한 level의 음표 동그라미에는 썸네일이 안보이고, Clear 해야 한다는 의미의 자물쇠 아이콘이 보인다.
* 음표 동그라미는 Clear한 level의 썸네일을 보여줘야 하기 때문에 크게 보인다.
- 음표 동그라미에 붙은 랜덤한 음표 기호는 작고 귀엽게 보인다.
* 음표 동그라미는 실제 악보의 음표처럼 오선보의 랜덤한 라인에 걸쳐있어야 한다.
- 모든 음표는 오선보 안에서 랜담한 높낮이를 가진다.
* PLAYER는 `lv1`부터 `lv30`까지 순서대로 Clear 해야 한다.

2. 현재까지 만든 `lv1`부터 `lv30`까지는 로그인 없이 누구나 플레이 할 수 있다.
- 지금은 아니지만, 이 후에 만들 `lv31` 부터는 유료 이용자에게만 공개시킬 예정이다.
- 그렇기 때문에, `lv1`부터 `lv30`까지의 Clear 여부는 서버가 아닌 클라이언트에서 관리해야 한다.
- 클라이언트에서 관리할 때, 사용자가 자신의 기록을 조작할 수 없도록 해야한다.

3. 반응형 기능이 구현되어야 하기 때문에, 화면의 `width`가 긴 경우, 오선보 위의 음표 개수가 늘어나고, 화면의 `width`가 짧은 경우, 오선보 위의 음표 개수가 줄어든다. 오선보 위의 음표 개수가 늘어나면 오선보의 개수가 줄어들고, 오선보 위의 음표 개수가 줄어들면 오선보의 개수가 늘어나야 한다.
- 디자인은 최대한 실제 악보스럽게 알아서 센스있게 잘 해라.

※ 현재 내가 첨부한 `myApp-Front`는 메인화면을 만들 `src/pages/main/main.html`, `src/pages/main/mainPage.js` 빈파일만 있다.
※ 현재 내가 첨부한 `myApp-Front`에서 메인화면을 만들면서, 추가/수정된 파일만 내가 다운로드 받을 수 있도록 첨부해라.
※ 게임은 전체적으로 파스텔톤으로 예뻐야 한다. 디자인과 에니메이션/모션도 최대한 깔끔하고 심플하고 고급스럽게 네가 알아서 만들해라.
* lv1 ~ lv30의 디자인 통일성은 유지하되, 최대한 고급스럽고 심플하게 디자인해라.
※ 지금 만드는 게임은 웹브라우저에서 실행되는 웹게임이다. 
* 그래서 dark mode를 지원해야 한다. dark mode에도 어울리는 예쁜 컬러여야 한다.
* 브라우저 사이즈를 늘렸다 줄였다 해도 정상적으로 비율로 게임을 할 수 있도록 반응형이어야 한다.























# lv30(`https://127.0.0.1:5173/lv30`) 설명 :

## lv30은 게임은 아니고, 동그라미 안에 텍스트 모션이 보여진 후, 동그라미를 터치하면 이벤트가 일어나는 화면이다.

1. `#appView`은 `width`가 `100vw`이고 `height`가 `100vh`다. 브라우저에서 제공하는 url 같은 영역에 가려져서는 안된다.
* lv30 진입 시, 빈 화면이 보인다.
* 18개의 동그라미가 꽃이 피듯 피어난다.
- 동그라미 안에는 각각 `T`, `h`, `a`, `n`, `k`, `y`, `o`, `u`, `f`, `o`, `r`, `p`, `l`, `a`, `y`, `i`, `n`, `g` 글자가 한 글자씩 들어있다.
- 텍스트는 `Thank you for playing` 을 한 글자씩 나눈 것이다.
- 그래서 `T`, `h`, `a`, `n`, `k`가 한 줄로 보이고, `y`, `o`, `u`가 한 줄로 보이고, `f`, `o`, `r`가 한 줄로 보이고, `p`, `l`, `a`, `y`, `i`, `n`, `g`가 한 줄 로 보인다.
- 랜덤한 위치에 둥실둥실 떠있는 4줄의 동그라미들이 보이는 것이다. 
* 18개의 동그라미 텍스트가 보여지는 순서는 랜덤하다.
* 18개의 동그라미는 자신의 모션이 끝난 후, 자신의 자리에서 둥실둥실 떠있는 모션이 있어야 한다.
* 각각의 동그라미 글자를 터치하면, 동그라미가 약간 커졌다가 본래 크기로 돌아오는 모션 이벤트가 있어야 한다.
- 동그라미를 터치하면, 동그라미 주위에 꽃잎이 터지는 듯한 예쁜 모션이 있어야 한다.
- 모든 동그라미는 각각의 다른 음을 가지고 있어서 터치하면 자신의 음을 내야 한다.
- `lv30` 진입 시, 빈 화면에서 피어날때도 자신의 음을 내야 한다.
* 화면 하단에 `To Be Continued` 문구가 예쁘게 떠있다.
* `To Be Continued` 문구 아래 `HOME` 버튼이 있다.
- `HOME` 클릭 시 메인화면으로 이동한다.

2. 게임이 아니기 때문에 lv1 ~ lv29 에 있는 게임 시작 전, 게임을 설명해주는 모션단계는 필요없다.

3. 반응형 기능이 구현되어야 하기 때문에, 화면의 `width`가 긴 경우, 오선보 위의 음표 개수가 늘어나고, 화면의 `width`가 짧은 경우, 오선보 위의 음표 개수가 줄어든다. 오선보 위의 음표 개수가 늘어나면 오선보의 개수가 줄어들고, 오선보 위의 음표 개수가 줄어들면 오선보의 개수가 늘어나야 한다.
- 디자인은 최대한 실제 악보스럽게 알아서 센스있게 잘 해라.

※ 현재 내가 첨부한 `myApp-Front`는 메인화면을 만들 `src/pages/main/main.html`, `src/pages/main/mainPage.js` 빈파일만 있다.
※ 현재 내가 첨부한 `myApp-Front`에서 메인화면을 만들면서, 추가/수정된 파일만 내가 다운로드 받을 수 있도록 첨부해라.
※ 게임은 전체적으로 파스텔톤으로 예뻐야 한다. 디자인과 에니메이션/모션도 최대한 깔끔하고 심플하고 고급스럽게 네가 알아서 만들해라.
* lv1 ~ lv30의 디자인 통일성은 유지하되, 최대한 고급스럽고 심플하게 디자인해라.
※ 지금 만드는 게임은 웹브라우저에서 실행되는 웹게임이다. 
* 그래서 dark mode를 지원해야 한다. dark mode에도 어울리는 예쁜 컬러여야 한다.
* 브라우저 사이즈를 늘렸다 줄였다 해도 정상적으로 비율로 게임을 할 수 있도록 반응형이어야 한다.


----------------------------------------------------------------------------

# 메인화면(`추가·수정된 파일만 다운로드`)에 몇 가지 수정사항이 있다.
* 지금 음표가 여러줄의 라인위에 떠있다.
- 라인만 있으면 `오선보`로 인식되지 않는다.
- 음표는 `오선보` 위에 떠있어야된다. `오선보`를 만들어라. 음표를 `오선보` 위에 띄워라.
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

# 오선보 수정(`오선보 수정 파일 다운로드`)은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
1. 현재 `Clear` 판정 로직에 오류가 있다.
* 예를 들어, 내가 `lv1`을 Clear 하고, `NEXT`와 `HOME` 버튼들이 보이는 상태에서, `HOME` 버튼 클릭 후, 메인화면으로 이동하면, `lv1`은 Clear 상태가 아니다. 
- `NEXT`와 `HOME` 버튼들이 보이는 상태에서, `NEXT` 버튼을 클릭 후 `lv2` 로 이동해야 `lv1` 이 Clear 상태가 된다.
* 이러면 안된다. 각각 level을 Clear했다면, 해당 Level은 Clear 판정을 받아야 한다.

----------------------------------------------------------------------------

# `Clear` 판정 로직 수정(`Clear 판정 수정 파일 다운로드`)은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* 현재 `Clear`한 level을 `localStorage`에 저장시키고 있다.
- `checksum`과 `cleared` 라는 key로 value를 저장시키고 있다.
* `checksum`과 `cleared`의 영어가 너무 직관적이라, 무엇을 의미하는지 유추하기가 쉽다.
- `checksum`과 `cleared`을 사용자가 알아볼 수 없는 문자로 난독화 시켜야 한다.
- `cleared`의 value인 `1, 2, 3 ... 30` 숫자도 각각 난독화 시켜야 한다.
* 그래서 사용자가 최대한 유추할 수 없도록 해야한다.

----------------------------------------------------------------------------

# `저장값 난독화 수정 파일 다운로드` 수정은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* 디자인 수정이 필요하다.
- `header.score-header`와 `footer.score-footer`는 없애라.
- `main#scoreSheet`에 이상한 흰색 테두리(`box-shadow`, `boder`, `boder-radius` 등등)가 있는데, 없애라.
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

# 

----------------------------------------------------------------------------

# 

----------------------------------------------------------------------------

# 

----------------------------------------------------------------------------


































# lv29(`https://127.0.0.1:5173/lv29`) 게임규칙 :

## lv29는 `Headbangers: Rhythm Royale` 라는 게임의 `That '70s Race` 파트와 유사하다. 둥근 턴테이블 원반이 있고, 회전하는 원반에 있는 node를 타이밍에 맞게 터치해야하는 리듬게임이다.

1. `#appView`은 `width`가 `100vw`이고 `height`가 `100vh`다. 브라우저에서 제공하는 url 같은 영역에 가려져서는 안된다.
* lv29 진입 시, 화면을 거의 가득 체우는 크기의 LP판이 있다.
- LP판의 지름은 화면의 세로가 길 경우, 화면의 가로 크기만 하다. 
- LP판의 지름은 화면의 가로가 길 경우, 화면의 세로 크기만 하다.
* LP판의 랜덤한 위치에 node가 심어져있다. 
- LP판의 중앙부터 LP판의 1/3에 해당하는 공간은 빈공간이다. 빈공간에는 예쁜 문양이 그려져있다.
- LP판의 빈공간 둘레부터 LP판의 전체 둘레까지 4단계의 원형 구간이 있다.
- 이 4단계 원형 구간의 랜덤한 위치에 node가 자리잡는다. 
- LP판의 빈공간 둘레부터 LP판의 전체 둘레까지 `1단계 원형 도넛 구간`, `2단계 원형 도넛 구간`, `3단계 원형 도넛 구간`, `4단계 원형 도넛 구간`이 있는 것이다.
* LP판의 중앙부터 하단까지 `｜` 모양을 가진 터치 라인이 있다. PLAYER는 LP판이 회전하면서 이 라인에 도착하는 node를 터치해야 한다.

2. LP판이 360° 회전하면서 터치라인과 node가 겹치는 순간, PLAYER는 정확한 타이밍에 node를 터치해야 한다.
- node의 터치와 관계없이 터치라인에 도착한 node는 사라진다. 사라지고 LP판의 랜덤한 위치에 다시 새로운 node가 생긴다.

3. PC의 경우 마우스로 동시에 2개 이상 node를 클릭 할 수 없기 때문에 터치구간에 도착하는 node는 1개씩이어야 한다.

4. `lv29`의 총 게임시간은 `30초` 이다.

5. 게임 시작 시, LP판은 천천히 회전한다. 시간이 지날수록 회전 속도가 빨라진다.
- 게임 시작 시, node의 개수가 적다. 시간이 지날수록 node의 개수가 늘어난다.

6. PLAYER가 node를 터치 시 :
* 터치라인에 도착한 타이밍에 맞게 터치했으면 성공판정이다.
* 터치라인에 도착해서 사라지기 전까지 터치를 못했거나, 터치라인에 닿기 전에 터치했다면 실패판정이다.

5. 성공, 실패와 관계없이 30초동안 게임은 계속된다. 맞는 규칙에 따라 타이밍에 맞게 모두 성공하면, 게임이 완료된 후 `NEXT` 버튼이 노출되고, 한 번이라고 실패하면 게임이 완료된 후 `RETRY` 버튼이 노출된다.
- `NEXT` 버튼 클릭 시 lv30으로 이동
- `RETRY` 버튼 클릭 시, START 버튼 누르는 단계없이 lv29 바로 재시작
- `NEXT`,`RETRY`은 항상 메인화면으로 이동하는 `HOME`과 함께 있어야 한다. 

6. lv1 ~ lv28 과 마찬가지로 게임 시작 전, 게임을 설명해주는 모션단계가 있어야 한다. 깔끔하고 세련되고 이해하기 쉽도록 네가 알아서 잘 만들어라.

※ 현재 내가 첨부한 `myApp-Front`는 lv29 게임을 만들 `src/pages/game/lv29/lv29Page.js`, `src/pages/game/lv29/lv29.html` 빈파일만 있다.
※ 현재 내가 첨부한 `myApp-Front`에서 lv29를 만들면서, 추가/수정된 파일만 내가 다운로드 받을 수 있도록 첨부해라.
※ 게임은 전체적으로 파스텔톤으로 예뻐야 한다. 디자인과 에니메이션/모션도 최대한 깔끔하고 심플하고 고급스럽게 네가 알아서 만들해라.
* lv1 ~ lv28의 디자인 통일성은 유지하되, 최대한 심플하게 디자인해라.
※ 지금 만드는 게임은 웹브라우저에서 실행되는 웹게임이다. 
* 그래서 dark mode를 지원해야 한다. dark mode에도 어울리는 예쁜 컬러여야 한다.
* 브라우저 사이즈를 늘렸다 줄였다 해도 정상적으로 비율로 게임을 할 수 있도록 반응형이어야 한다.

----------------------------------------------------------------------------

# lv29는 내가 의도했던 게임이다. 그런데 몇 가지 수정사항이 있다.
* 예쁜 문양이 있는 LP판의 빈공간부터 LP판의 전체 둘레까지 4단계의 구간만 있어야 한다. 지금은 너무 많다.
- 대충 현재 13단계의 원형 도넛이 있는것으로 파악되었다.
- 현재 셀수없이 많은 단계에서 4단계로 줄면 node의 크기도 그만큼 커져야 한다. 
- 원형 node는 각 단계의 내부에 위치하고 있어야 한다.
- 원형 node의 지름은 각 단계 도넛의 두께만큼이어야 한다.
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

# LP판 4단계 수정(`lv29 4단계 도넛 구간 수정 파일 다운로드`)은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* 현재 4단계의 도넛 원형 구간으로 잘 수정되었다.
* 그런데 터치구간에서 각 단계에 있는 node를 맞는 타이밍에 터치하면, 모두 같은 음이 들린다.
* 4단계에 있는 node는 각각 다른 높낮이를 가진 음이어야 한다.
* 단, PLAYER가 터치에 실패할 경우 뭔가.. `실수했어요!` 라는 느낌의 부정적인 음이 들려야 한다.


----------------------------------------------------------------------------

# 사운드 수정(`lv29 단계별 음높이 및 실패음 수정 파일 다운로드`)은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* `lv29` 진입 시, 게임을 설명해주는 단계에서, LP판이 회전하는 것이 아니라, 위에서 오른쪽 아래로 이동한다.
- LP판은 360° 회전해야 하고, LP판 위에 있는 node는 LP판 중심축을 기준으로 회전해야 한다.
* 게임 설명 단계 후, `HOW TO PLAY WATCH` `HOW TO PLAY TOUCH` 텍스트가 나오는 단계에서, LP판이 회전하는 것이 아니라, 위에서 오른쪽 아래로 이동한다.
- LP판은 360° 회전해야 하고, LP판 위에 있는 node는 LP판 중심축을 기준으로 회전해야 한다.
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

# `lv29 설명 단계 LP판 중심 회전 수정 파일 다운로드` 수정은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* 현재 게임이 진행될 수록 LP판의 회전 속도가 점차적으로 증가한다.
* 그런데 너무 빨라져서 게임의 난이도가 상당하다.
- 현재 게임 시작부터 끝까지 LP판 회전 속도 증가율이 `0 ~ 10` 이라면 `0 ~ 5` 속도로 느리게 수정해라.
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

# `lv29 LP판 회전 속도 증가율 완화 파일 다운로드` 수정은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* 내가 계속 TEST 중인데, node안에 있는 불투명한 작은 원이 터치 구간에 머물렀을 때 node를 터치해야 성공판정인 것으로 보여진다.
* 이렇게 되면 게임 난이도가 상당히 높아진다.
* PLAYER는 분명 node가 터치구간 라인에 머무르는 순간에 터치했는데, 실패판정이 되어 게임에 대한 불신이 생길 수 있다.
* PLAYER가 node를 터치할 때, node 전체가 터치라인에 머물러만 있다면 성공판정이 될 수 있도록 수정해라.
* PLAYER가 타이밍에 맞게 터치하지 못한 node는 node전체가 터치라인을 빠져나가면 사라지는 방식으로 수정해라.
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

# `lv29 노드 전체 겹침 판정 수정 파일 다운로드` 수정은 잘 되었다. 그런데 몇 가지 수정사항이 있다.
* 내가 계속 TEST 중인데, 이상하게 맞는 타이밍에 node를 터치해도 간혈적으로 터치가 무시되는 경우가 있다. 그래서 맞게 터치했다고 생각했는데 실패판정이 나버린다.
- 이런 경우 PLAYER는 똥망겜이라고 생각한다. 그리고 떠나버린다. 그러니 판정의 정확도를 더욱더 면밀히 검토하고, 실수없이 완벽하게 수정해라. 
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 뭐 하나 수정했다고 이전에 되던게 안되는 실수는 절대 하지마라!
※ 전체 코드를 정확히 분석해라. 내 요구사항을 정확히 이해해라. 그래서 오류없이 정확히 수정해라!

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------
----------------------------------------------------------------------------

#

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------
----------------------------------------------------------------------------

#

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------
----------------------------------------------------------------------------

#

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------

#

----------------------------------------------------------------------------












